use_frameworks!

platform :ios, '10.0'

target 'commonTools_Example' do
  pod 'commonTools/framework', :path => '../'

  target 'commonTools_Tests' do
    inherit! :search_paths

    pod 'Specta'
    pod 'Expecta'
    pod 'FBSnapshotTestCase'
    pod 'Expecta+Snapshots'
  end
end
